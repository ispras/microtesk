VAR RS : 64;
VAR immediate : 32;

// immediate is 16bit
ASSERT immediate >=+ 0 AND immediate <+ 65536; 

temp := RS[63].RS + (65)immediate;

SITUATION temp[64] # temp[63] IS IntegerOverflow.
