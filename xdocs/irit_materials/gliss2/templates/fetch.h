/* Generated by gep ($(date)) copyright (c) 2008 IRIT - UPS */

#ifndef GLISS_$(PROC)_INCLUDE_$(PROC)_FETCH_H
#define GLISS_$(PROC)_INCLUDE_$(PROC)_FETCH_H


#if defined(__cplusplus)
extern  "C"
{
#endif

#include "api.h"
#include "config.h"

#define $(PROC)_FETCH_STATE
#define $(PROC)_FETCH_INIT(s)
#define $(PROC)_FETCH_DESTROY(s)

/* fetch structure */
struct $(proc)_fetch_t
{
	/* the memory which we are supposed to fetch from, useful on Harvard architectures where we deal with several memories */
	$(proc)_memory_t *mem;
$(if is_multi_set)/* state used to determine correct fetch */
	$(proc)_state_t *state;$(end)
};

#if defined(__cplusplus)
}
#endif

#endif /* GLISS_$(PROC)_INCLUDE_$(PROC)_FETCH_H */
