let X = 10

#include "include_one.nml"
#include "include_two.nml"

mode ALL_MODES = A | B | C
op ALL_OPS = ADD | SUB | MOV

op instruction(x : ALL_MODES, y : ALL_OPS)
  syntax = ""
  image  = ""
  action = {}

